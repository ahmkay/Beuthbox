<% include ./../partials/head %>

<body>
    <header>
        <% include ./../partials/header %>
    </header>

    <div class="container-fluid content">

        <h3 class="carousel-title">Channels</h3>


        <div class="row">


            <% channels.forEach(function(channel, k){ %>
            <div class="col-md-6 category-single">


                <div class="categoryContainer">
                    <a href="/channel/<%= channel._id %>">
                        <img src="<%= apiUrl %>/channel<%= channel.imagepath %>" class="categoryImage">
                    </a>
                    <a href="/channel/<%= channel._id %>" class="title link-unstyled titleCatNew"><%= channel.name %></a>
                    <p class="descriptionSmall"><%= channel.description %></p>
                </div>

                <!--

        <div class="col-md-6">
            <img src="<%= apiUrl %>/channel<%= channel.imagepath %>">
        </div>
        <div class="col-md-6 info">
            <img src="<%= apiUrl %>/channel<%= channel.iconpath %>" class="user">
            <a href="/channel/<%= channel._id %>" class="title link-unstyled"><%= channel.name %></a>
            <p class="description"><%= channel.description %></p>
        </div>   
    </div> 
-->
            </div>
            <% }) %>


        </div>

    </div>


    <% include ./../partials/footer %>